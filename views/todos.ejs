<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Workout Tracker!</title>
    <link rel="stylesheet" href="css/style.css">
</head>
<body>
    <h1>Tracking time</h1>
    <div id="countdown"></div>
    <ul>
    <% todos.forEach( el => { %>
            <li class='todoItem' data-id='<%=el._id%>'>
                <span class='<%= el.completed === true ? 'completed' : 'not'%>'><%= el.todo %></span>
                <span class='del'> Delete </span>
            </li>
    <% }) %>    
    </ul>

    <h2><%= user.userName %> has <%= left %> wokouts left to do.</h2>

    <form action="/todos/createTodo" method='POST'>
        <input type="text" placeholder="Workout Item" name='todoItem'>
        <input type="submit">
    </form>
    <form action="">
    <input type="number" placeholder="Enter time for workout" name="timeForWorkout">
    <input id="timerSubmit" type="submit">
    </form>
    <a href="/logout">Logout</a>

    <script src="js/main.js"></script>
</body>
</html>